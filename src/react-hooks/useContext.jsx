import React from "react";

export const UseContext = () => {
  return (
    <div>
      <h4 className="text-sm font-semibold">Use Context</h4>
      <p>Use Context hook is use to share data across entire Application or global component.</p>
    </div>
  );
};
export default UseContext;
